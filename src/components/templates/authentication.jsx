import React from "react";

import { StyledAuthentication } from "./style";

const AuthenticationTemplate = ({ children }) => {
  return <StyledAuthentication>{children}</StyledAuthentication>;
};

export default AuthenticationTemplate;
