import React from "react";

import { StyledApplication } from "./style";

const ApplicationTemplate = ({ children }) => {
  return <StyledApplication>{children}</StyledApplication>;
};

export default ApplicationTemplate;
